[project]
name = "meshcall-demo-showcase"
version = "0.1.0"
description = "The complete recommended MeshCall API showcase"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "meshcall",
    "pydantic>=2.11",
]

[project.scripts]
demo = "showcase_demo.run:main"

[tool.uv.sources]
meshcall = { path = "../../meshcall-py" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/showcase_demo"]
